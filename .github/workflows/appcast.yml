# This is a basic workflow to help you get started with Actions

name: Appcast


on:
  
  workflow_dispatch:

jobs:

  build:
    # The type of runner that the job will run on
    runs-on: macos-latest


    steps:
    
#       - name: test
#         run: |
#           build=123
#           signature=UyGJkUXMfP21nmolTrtqGM8Gg2
#           xz=`echo ${signature%\" *}`
#           echo "<!--$build,${xz#*=\"}-->"
    
    
      - name: Rebuild
        run: |
          gh api \
            -H "Accept: application/vnd.github.v3+json" \
            /repos/${{ github.repository }}/pages/builds/latest
        env:
          GH_TOKEN: ${{ secrets.PERSONAL_TOKEN }}
          

#           curl https://api.github.com/repos/${{ github.repository }}/pages/builds \
#           -X POST \
#           -H "Authorization: token ${{ secrets.PERSONAL_TOKEN }}" \
#           -H "Accept: application/vnd.github.v3+json" \
          
